import { render, fireEvent, leftClick } from '@testing-library/react';
import App from './App';
import { Provider } from "./context";

describe('This will test User component', () => {
  test('render users suggestion after type on the search-box', () => {
   
  });

  
})